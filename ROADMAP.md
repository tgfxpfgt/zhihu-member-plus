# 知乎盐选会员增强助手 — 功能候选清单（Roadmap）

> **维护约定**：本文件是功能/设计的候选清单，每次迭代从这里选取条目实施；实施完成后将条目移入「已实现记录」并在 manifest/lite 同步升版本。
> **调研基线**：2026-08-31，对标 X.I.U《知乎增强》v2.3.32（2026-08 更新）、《沉浸式知乎》、《知乎 AI 总结助手》、《朕知道了-知乎拒绝废话》、Zhihu-Smart-Assistant。
> **难度标记**：★☆☆ 小（半天内）｜★★☆ 中（1-2 天）｜★★★ 大（需架构扩展）

---

## 当前能力基线（v1.2.0 已实现）

会员增强（去遮挡/高清图/标签/无干扰/全屏）、阅读样式（字体字号行距/背景/夜间/沉浸/分页）、10 套美化主题、宽屏适配、净化屏蔽（广告/直播/课程/带货/咨询/横幅/视频/低赞）、关键词与作者黑名单、评论折叠、效率增强（直链还原/完整时间/代码复制/一键收起/关登录弹窗）、目录/进度记忆/本地标签/Markdown 导出、字数统计、悬浮菜单、快捷键（Alt+Z/T/N/I/S）、性能节流、首次引导 toast。

---

## P0 — 高价值 + 与现有架构契合，建议下版实施

### A1. 回答聚合侧栏（问题页）★★★
自动提取当前问题下全部回答，在右侧生成锚点导航：按赞同数降序排列，卡片式显示作者+摘要，点击滚动定位，支持「按时间排序」切换与一键复制单条回答。对标 Zhihu-Smart-Assistant 内容聚合模块，是用户最直观的"长问题页救星"。
**依赖**：新增 content 模块（如 `aggregate.js`）；回答数据可从 DOM 提取，无需 API。虚拟滚动下需 observer 增量收集。

### A2. TTS 听书模式 ★★☆
对盐选故事/专栏/长回答调用浏览器本地 `speechSynthesis` 朗读正文：悬浮菜单加「▶ 听书」入口，底部悬浮控制条（播放/暂停/语速/音色），朗读进度跟随滚动。**零 API 依赖、零成本**，与"盐选会员"定位强契合（听故事场景）。
**依赖**：无外部依赖；需处理分段朗读与中文音色筛选。

### A3. 选中文字 → 加入屏蔽词 ★☆☆
划选文字后浮出小按钮「＋屏蔽」，点击写入 `purify.blockKeywords` 并即时过滤；popup 黑名单同步可见可删。对标 X.I.U v2.3.26 新增的同名功能，黑名单的录入体验提升立竿见影。
**依赖**：无，纯前端；可复用 ZMPStorage + filter 模块。

### A4. popup 设置项搜索框 ★☆☆
设置项已 30+，popup 顶部加搜索输入，实时过滤各 tab 的 setting-item/setting-row（命中项跨 tab 高亮或聚合显示）。纯 popup 端改造。

### A5. GitHub Actions CI ★☆☆
push 时自动跑：① 全部 JS `node --check`；② manifest/lite/popup 徽章三处版本一致性断言；③ 打包 zip 供 Chrome Web Store 直接上传（artifact）。
**依赖**：把现有手工校验命令固化成 `scripts/check.js` + workflow yml。

---

## P1 — 计划跟进（第二梯队）

### B1. AI 总结按钮（自带 Key）★★★
回答/文章下方加「✨ 总结」按钮：OpenAI 兼容格式（通义/DeepSeek/OpenAI/本地中转），用户在 popup 填 API Key + 选模型 + 自定义 Prompt；流式输出到卡片，缓存最近 5 次结果防重复调用，总结前确认防误触。对标《知乎 AI 总结助手》与 Zhihu-Smart-Assistant，是当前知乎增强赛道最热方向。
**依赖**：需 `host_permissions` 增加模型 API 域名或后台代理；Key 仅存本地。建议做成默认关闭的可选功能。

### B2. 屏蔽指定类别 + 按页面独立配置 ★★☆
屏蔽项从「视频」扩展到：文章、想法、盐选卡片、关注动态；且首页/关注页/热榜/搜索页可对各类屏蔽项独立开关（X.I.U 已验证用户有此诉求）。配置结构变为 `purify.blockTypes[pageType][type]`。
**依赖**：storage 结构迁移（需兼容旧配置）+ popup 改造，是本梯队工作量最大项。

### B3. 一句话提炼 + 内容性质判别 ★★★
长回答折叠为一句话核心观点（本地规则版：取首段/加粗句/结论词启发式，免费可用），可选接 AI 升级；并用启发式给内容打「知识/观点/软文/情绪」标签着色提示，帮助识别软文。对标《朕知道了》。
**依赖**：无 API 也可做本地规则版；AI 版依赖 B1。

### B4. 本地收藏夹 + 导出 ★★☆
内容卡片加「☆ 收藏」按钮，存入本地收藏夹（标题/作者/链接/摘要/时间 + 自定义标签），popup 新增「收藏」tab 管理、搜索、按标签过滤，一键导出 Markdown/HTML（Obsidian/Notion 友好）。与现有 tools.js 本地标签能力可合并升级。
**依赖**：storage 新增收藏 section；注意容量与去重。

### B5. 阅读统计看板 ★★☆
本地统计每日阅读字数/篇数/时长，popup 显示近 7 天简版柱状图；可选「阅读偏好」统计（常看话题词频），呼应"信息茧房"方向但不做推荐干预。
**依赖**：统计埋点分散在各模块（tools/performance），需收敛到一个统计入口。

### B6. 自动滚动（匀速阅读）★☆☆
悬浮菜单加「自动滚动」，速度可调（像素/秒），鼠标悬停或触底自动暂停；适合盐选长文解放双手。实现小、无依赖。

### B7. 新标签页打开链接（可选）★☆☆
知乎默认当前页跳转，翻列表时容易丢位置。提供开关将信息流/搜索/热榜链接改为 `target=_blank`。X.I.U 同类功能，实现极小。

### B8. 阅读时长提醒 ★☆☆
连续阅读 N 分钟（可配）toast 提醒休息，配合现有 toast 组件，成本低。

---

## P2 — 探索项（有明确价值但优先级靠后 / 依赖较重）

| # | 功能 | 说明 | 难度 |
|---|------|------|------|
| C1 | 默认收起回答 | 问题页进入时全部收起（X.I.U 核心功能之一，可配合 A1 聚合侧栏使用） | ★☆☆ |
| C2 | 显示问题作者 / 直达问题按钮 | 列表页卡片显示提问者；「直达问题」跳过回答聚合页 | ★★☆ |
| C3 | 屏蔽低评论数 | 与低赞屏蔽并列的评论数阈值过滤（X.I.U v2.3.17 已验证需求） | ★☆☆ |
| C4 | 净化搜索页 | 搜索页「热门搜索/最新讨论」等模块隐藏开关 | ★☆☆ |
| C5 | 图片增强 | hover 显示原图预览 + 夜间模式图片智能降亮度（已有简单 filter，可细化） | ★★☆ |
| C6 | 划词表达本 | 选中句子一键收藏，导出 Markdown（对标《沉浸式知乎》表达本，外语学习向） | ★★☆ |
| C7 | 快捷键自定义 | popup 内录制式改键（当前 Alt+Z/T/N/I 固定） | ★★☆ |
| C8 | popup 深色主题 | 跟随 `prefers-color-scheme` 的 popup 双主题 | ★☆☆ |
| C9 | 当前页面生效功能面板 | popup 显示「此页面实际生效了哪些功能」，降低配置困惑 | ★★☆ |
| C10 | 导出 EPUB/PDF 优化 | 现有 Markdown 导出扩展为 EPUB；打印隐藏站点 UI 的 @media print 样式 | ★★☆ |
| C11 | CHANGELOG + README 截图 | 建立变更日志与带截图的 README，上架商店必备 | ★☆☆ |
| C12 | eslint 基础接入 | 统一风格、防低级错误，接入 CI | ★☆☆ |

---

## 暂不做（明确排除 + 原因）

- **设备同步/云备份**：需要账号体系，超出纯本地工具边界。
- **视频/图片下载**：版权风险。
- **自动点击「不感兴趣」/自动点赞关注等账号行为**：触碰账号安全红线。
- **内容改写、自动评论**：违反平台规则方向。
- **全站翻译**：与产品定位（盐选会员阅读增强）无关，中文场景价值低。

---

## 已实现记录

| 版本 | 日期 | 内容 |
|------|------|------|
| v1.1.0 | 2026-08-28 | 对标调研 8 项：直链还原/完整时间/代码复制/一键收起/关登录弹窗/低赞屏蔽/视频屏蔽/快捷键 |
| v1.2.0 | 2026-08-31 | popup 状态同步/标签记忆/导入反馈、首次引导 toast、深色主题浅色表面适配 |
